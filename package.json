{
  "name": "smartseason-field-monitoring-system",
  "private": true,
  "type": "module",

  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  }
}
